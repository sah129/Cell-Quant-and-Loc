
```{r setup, include=FALSE}
source('src/functions.R')
source('src/Main.R')
datasetpath <-"Datasets/Figure 2C Bul Data/Bul1-del+Bul2-del"
```

"Datasets/Figure 2C Bul Data/Bul1-del"
"Datasets/Figure 2C Bul Data/Bul1-del+Bul2-del"

```{r}
res <- pipeline_git1(datasetpath, testing=FALSE, gui=FALSE, progress=NULL)
```

```{r}
#groups = list("9ArrD 316", "9ArrD Aiy1", "9ArrD Aly2", "9ArrD Csr2", "9ArrD Ecm21", "9ArrD Ldb19", "9ArrD Rod1", "9ArrD Rog3", "9ArrD Ylr392c", "9ArrDYgr068c", "WT 316")
#groups = list("9Arr-del","aly1-del") 
groups = list("Bul1-del")
#groups = list("aly1-del_aly2-del", "aly2-del", "BY4741")
#groups = list("9ArrD 316", "9ArrD Aiy1", "9ArrD Aly2", "9ArrD Csr2", "9ArrD Ecm21", "9ArrD Ldb19", "9ArrD Rod1", "9ArrD Rog3", "9ArrD Ylr392c", "9ArrDYgr068c")


sort_data(res, groups)
```

```{r}
r <- res[[2]]
```


```{r}
get_display_img(df = r$df,
                membranes = r$membranes, 
                col_membranes = 'white', 
                vacuoles = r$vacuoles, 
                col_vacuoles ='yellow', 
                removed = r$removed,
                closed_vacuoles = TRUE, 
                img = r$channels$gfp, 
                showRemoved = FALSE, 
                showMemLabels = TRUE, 
                showVacLabels = TRUE)
```





##############################################################3



```{r}
get_image_names <- function(res)
{
  image_names = vector(mode = "list",length = length(res))
  for(i in 1:length(res))
  {
    image_names[i] <- res[[i]]$filename
  }
  return(image_names)
}
```




```{r}
get_max_cells <- function(res)
{
  max = -1
  for(i in 1:length(res))
  {
    if( max < nrow(res[[i]]$df["cell_mpi"]))
      max = nrow(res[[i]]$df["cell_mpi"])
  }
  return(max)
}

```


```{r}
max_cells <- get_max_cells(res)

```

```{r}
create_dummy_row <- function(row_text, m)
{
  df <- data.frame(Image = row_text)
  ids <- vector("list", m)
  for(j in 1:m)
  {
   x <- data.frame(NA)
    names(x) <- j
    ids[[j]]<- x
  }
  df <- cbind(df, ids)
  df
}
```



